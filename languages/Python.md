# Python

## OOP

1. `__init__(self, args)`
    1. Call once the object is created

2. private variables
    1. name starting with '__'.


## Numpy

### Create New Array
1. np.full(shape, value, dtype)
    1. full the array with the same value

